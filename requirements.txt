# Clerk-san dependencies for Python 3.11.
#
# This is the installable base used by the API and worker image. Version ranges
# pin the compatible major line while allowing security patch updates.

# API and configuration
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
streamlit>=1.40,<2.0
pydantic>=2.13.4,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0
python-multipart>=0.0.18,<1.0

# PostgreSQL persistence and local-model HTTP client
SQLAlchemy[asyncio]>=2.0.36,<3.0
asyncpg>=0.30,<1.0
aiosqlite>=0.20,<1.0
pgvector>=0.3.6,<1.0
httpx>=0.28,<1.0

# Bounded ingest, OCR adapters, and document formats
python-magic>=0.4.27,<1.0
pypdf>=6.16,<7.0
pypdfium2>=5.13,<6.0
Pillow>=12.3,<13.0
mammoth>=1.8,<2.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0
PyYAML>=6.0,<7.0
pandas>=2.2,<3.0

# Duplicate detection, fixture generation, and benchmark memory metrics
ImageHash>=4.3,<5.0
RapidFuzz>=3.10,<4.0
Jinja2>=3.1,<4.0
psutil>=6.1,<8.0

# Test support. Kept in the base manifest so a fresh Python 3.11 environment
# can execute the plan's pytest suites without a second dependency file.
pytest>=9.0.3,<10.0
pytest-asyncio>=1.3,<2.0
respx>=0.22,<1.0
ruff>=0.8,<1.0

# The D1 OCR candidates (YomiToku and PaddleOCR) remain intentionally out of
# the base image: their CPU packages are platform-specific and deployment selects
# the supported engine on the target machine. Playwright is likewise an eval-
# only dependency and must not be added to the API/worker image.
